(()=>{'use strict';const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)],root=document.documentElement;const loader=$('#loader');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;setTimeout(()=>{if(loader){loader.style.opacity='0';loader.style.pointerEvents='none';setTimeout(()=>loader.remove(),500)}},reduce?150:650);
const progress=()=>{const max=Math.max(1,document.documentElement.scrollHeight-innerHeight);root.style.setProperty('--progress',Math.min(1,scrollY/max))};addEventListener('scroll',progress,{passive:true});progress();
const cursor=$('.cursor');let px=innerWidth/2,py=innerHeight/2,cx=px,cy=py,raf=0;if(cursor&&!reduce&&innerWidth>900){addEventListener('pointermove',e=>{px=e.clientX;py=e.clientY;root.style.setProperty('--mx',`${(e.clientX/innerWidth)*100}%`);root.style.setProperty('--my',`${(e.clientY/innerHeight)*100}%`)},{passive:true});const move=()=>{cx+=(px-cx)*.16;cy+=(py-cy)*.16;cursor.style.left=cx+'px';cursor.style.top=cy+'px';raf=requestAnimationFrame(move)};raf=requestAnimationFrame(move);$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;el.style.transform=`translate3d(${x*5}px,${y*4}px,0)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08});$$('.reveal').forEach(x=>io.observe(x));
const visual=$('#heroVisual');if(visual&&!reduce){visual.addEventListener('pointermove',e=>{const r=visual.getBoundingClientRect(),tx=(e.clientX-r.left)/r.width-.5,ty=(e.clientY-r.top)/r.height-.5;visual.style.setProperty('--rx',`${ty*-5}deg`);visual.style.setProperty('--ry',`${tx*7}deg`)});visual.addEventListener('pointerleave',()=>{visual.style.setProperty('--rx','0deg');visual.style.setProperty('--ry','0deg')})}
$$('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{if(reduce||innerWidth<900)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--card-x',`${(x+.5)*100}%`);el.style.setProperty('--card-y',`${(y+.5)*100}%`);el.style.transform=`perspective(900px) rotateX(${-y*3}deg) rotateY(${x*4}deg) translateY(-7px)`});el.addEventListener('pointerleave',()=>{el.style.transform='';el.style.removeProperty('--card-x');el.style.removeProperty('--card-y')})});
const menu=$('#menu'),toggle=$('.menu-toggle');toggle?.addEventListener('click',()=>{const open=!menu.classList.contains('open');menu.classList.toggle('open',open);document.querySelector('.nav').classList.toggle('menu-open',open);toggle.setAttribute('aria-expanded',String(open))});$$('#menu a').forEach(a=>a.addEventListener('click',()=>{menu?.classList.remove('open');document.querySelector('.nav')?.classList.remove('menu-open');toggle?.setAttribute('aria-expanded','false')}));
const secData={red:{k:'RED TEAM',t:'OFFENSIVE SECURITY',x:'بررسی سطح حمله، تست امنیت وب و شناسایی ریسک‌های قابل‌اثبات در محیط مجاز پروژه.',p:['Web Security Testing','Vulnerability Research','Attack Simulation'],n:'01'},blue:{k:'BLUE TEAM',t:'DEFENSIVE SECURITY',x:'طراحی کنترل‌های دفاعی، مشاهده‌پذیری، تحلیل لاگ و پاسخ به رخداد با تمرکز بر کاهش ریسک.',p:['Threat Detection','Log Analysis','Incident Response'],n:'02'},purple:{k:'PURPLE TEAM',t:'SECURITY VALIDATION',x:'اتصال حمله و دفاع: سنجش اینکه کنترل‌های دفاعی واقعاً حمله‌های مدل‌شده را تشخیص می‌دهند یا نه.',p:['Detection Engineering','Attack → Detect → Fix','Security Validation'],n:'03'},dev:{k:'DEVSECOPS',t:'SECURE SHIP',x:'امنیت را وارد چرخه توسعه می‌کنیم؛ از code review و dependency hygiene تا CI/CD security.',p:['Secure Development','Dependency Security','Security Automation'],n:'04'}};
const setSec=k=>{const d=secData[k];if(!d)return;$('#secKicker').textContent=d.k;$('#secTitle').textContent=d.t;$('#secText').textContent=d.x;$('#secIndex').textContent=d.n;const box=$('#secPoints');box.replaceChildren(...d.p.map(t=>{const b=document.createElement('b');b.textContent=t;return b}));$$('.sec-tab').forEach(b=>{const on=b.dataset.sec===k;b.classList.toggle('active',on);b.setAttribute('aria-selected',String(on))})};$$('.sec-tab').forEach(b=>b.addEventListener('click',()=>setSec(b.dataset.sec)));
const modal=$('#modal'),title=$('#modalTitle'),text=$('#modalText');const close=()=>{modal?.classList.remove('open');modal?.setAttribute('aria-hidden','true');document.body.style.overflow=''};$$('.project-card').forEach(card=>card.addEventListener('click',e=>{if(e.target.closest('a'))return;title.textContent=card.dataset.title||'PROJECT';text.textContent=card.dataset.desc||'';modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';$('#close')?.focus()}));$('#close')?.addEventListener('click',close);modal?.addEventListener('click',e=>{if(e.target===modal)close()});addEventListener('keydown',e=>{if(e.key==='Escape')close()});
$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id==='#')return;const target=$(id);if(target){e.preventDefault();target.scrollIntoView({behavior:reduce?'auto':'smooth',block:'start'})}}));window.addEventListener('beforeunload',()=>cancelAnimationFrame(raf));})();