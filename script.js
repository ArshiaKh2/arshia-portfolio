const glow=document.querySelector('.cursor-glow');const scene=document.querySelector('#scene');let mx=.5,my=.5,tx=.5,ty=.5,scrollY=0;const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));window.addEventListener('pointermove',e=>{tx=e.clientX/window.innerWidth;ty=e.clientY/window.innerHeight;if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px';glow.style.opacity='1'}});const magnetic=[...document.querySelectorAll('.btn,.top-cta,.links a,.work-card,.skill')];magnetic.forEach(el=>{el.addEventListener('pointermove',e=>{if(innerWidth<800)return;const r=el.getBoundingClientRect(),x=(e.clientX-(r.left+r.width/2))/r.width,y=(e.clientY-(r.top+r.height/2))/r.height;el.style.setProperty('--mx',(x*18)+'px');el.style.setProperty('--my',(y*14)+'px');el.style.transform=`translate3d(${x*12}px,${y*10}px,28px) rotateX(${-y*4}deg) rotateY(${x*5}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')});function render(){mx+=(tx-mx)*.07;my+=(ty-my)*.07;if(scene&&innerWidth>900){const r=scene.getBoundingClientRect(),x=(mx*innerWidth-r.left)/r.width-.5,y=(my*innerHeight-r.top)/r.height-.5;scene.style.transform=`translate3d(0,${clamp(scrollY*.035,0,32)}px,0) rotateX(${clamp(-y*9+scrollY*.018,-12,12)}deg) rotateY(${x*12}deg) scale(${1+clamp(scrollY/7000,0,.035)})`;scene.style.setProperty('--hero-depth',scrollY+'px')}requestAnimationFrame(render)}render();const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const sats=[...document.querySelectorAll('.sat')],cards=[...document.querySelectorAll('.floating-card')],orbs=[...document.querySelectorAll('.mini-orb')];let t=0;function orbit(){t+=.008;sats.forEach((el,i)=>{const a=t*(i%2?-1:1)+i*1.7;el.style.transform=`translate3d(${Math.sin(a)*14}px,${Math.cos(a*1.2)*12}px,${70+Math.sin(a)*35}px) rotate(${a*18}deg)`});cards.forEach((el,i)=>{el.style.transform=`translate3d(${Math.sin(t*(i%2?-.55:.7))*9}px,${Math.cos(t*.8+i)*10}px,${75+Math.sin(t+i)*12}px)`});orbs.forEach((el,i)=>{const a=t*(i?-.8:1.1)+i;el.style.transform=`translate3d(${Math.cos(a)*55}px,${Math.sin(a)*42}px,${120+Math.sin(a)*30}px)`});requestAnimationFrame(orbit)}orbit();function openModal(title,text){const m=document.querySelector('.modal');if(!m)return;document.querySelector('#modalTitle').textContent=title;document.querySelector('#modalText').textContent=text||'اینجا فقط یک نمونه نیست؛ ایده، طراحی، مهندسی، تعامل و جزئیات در کنار هم قرار گرفته‌اند تا یک تجربه دیجیتال حرفه‌ای ساخته شود.';m.classList.add('open');document.body.style.overflow='hidden'}function closeModal(){document.querySelector('.modal')?.classList.remove('open');document.body.style.overflow=''}document.querySelectorAll('.work-card').forEach((el,i)=>el.addEventListener('click',e=>{if(el.getAttribute('href'))return;const title=el.querySelector('h3,strong')?.textContent?.trim()||`Project ${i+1}`;openModal(title,'یک کیس‌استادی تعاملی از روند کار: تحلیل نیاز، معماری تجربه، طراحی رابط، پیاده‌سازی، بهینه‌سازی و تحویل نهایی. این پروژه برای نمایش کیفیت اجرا و نگاه محصول‌محور ساخته شده است.')}));document.querySelector('.modal-close')?.addEventListener('click',closeModal);document.querySelector('.modal')?.addEventListener('click',e=>{if(e.target.classList.contains('modal'))closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});let last=0;window.addEventListener('scroll',()=>{scrollY=window.scrollY;document.documentElement.style.setProperty('--scroll-y',scrollY+'px');const p=clamp(scrollY/(document.documentElement.scrollHeight-innerHeight||1),0,1);document.documentElement.style.setProperty('--scroll-progress',p);document.body.style.setProperty('--scroll-glow',`${p*100}%`);if(Math.abs(scrollY-last)>4){document.documentElement.classList.toggle('scrolled',scrollY>80);last=scrollY}},{passive:true});