(()=>{const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const loader=$('#loader');setTimeout(()=>{loader.style.opacity='0';loader.style.pointerEvents='none';setTimeout(()=>loader.remove(),800)},1100);const glow=$('.cursor-glow'),dot=$('.cursor-dot'),root=document.documentElement;let px=innerWidth/2,py=innerHeight/2,sy=0;addEventListener('pointermove',e=>{px=e.clientX;py=e.clientY;if(glow){glow.style.left=px+'px';glow.style.top=py+'px'}if(dot){dot.style.left=px+'px';dot.style.top=py+'px'}});$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{if(innerWidth<800)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;el.style.transform=`translate3d(${x*9}px,${y*7}px,18px) rotateX(${-y*3}deg) rotateY(${x*3}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')});$$('.reveal').forEach(el=>new IntersectionObserver(es=>es.forEach(x=>x.isIntersecting&&x.target.classList.add('visible')),{threshold:.12}).observe(el));addEventListener('scroll',()=>{sy=scrollY;const p=Math.min(1,sy/(document.body.scrollHeight-innerHeight||1));root.style.setProperty('--progress',p);if($('.progress'))$('.progress').style.width=p*100+'%'},{passive:true});const canvas=$('#heroCanvas'),stage=$('#stage');if(canvas&&window.THREE){const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.8));const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.1,100);camera.position.z=7;const group=new THREE.Group();scene.add(group);scene.add(new THREE.AmbientLight(0xffffff,2.5));const key=new THREE.PointLight(0x55ccff,12,20);key.position.set(3,4,6);scene.add(key);const violet=new THREE.PointLight(0x9270ff,10,16);violet.position.set(-4,-2,3);scene.add(violet);const core=new THREE.Mesh(new THREE.IcosahedronGeometry(1.15,4),new THREE.MeshPhysicalMaterial({color:0x75dff5,metalness:.18,roughness:.18,clearcoat:1,emissive:0x3159aa,emissiveIntensity:.25}));group.add(core);group.add(new THREE.Mesh(new THREE.IcosahedronGeometry(1.34,2),new THREE.MeshBasicMaterial({color:0x5578ff,wireframe:true,transparent:true,opacity:.28})));for(let i=0;i<5;i++){const r=new THREE.Mesh(new THREE.TorusGeometry(1.7+i*.22,.008,12,160),new THREE.MeshBasicMaterial({color:i%2?0x9270ff:0x20c8e8,transparent:true,opacity:.42}));r.rotation.set(.7+i*.3,.3+i*.45,i*.7);group.add(r)}const n=1100,pos=new Float32Array(n*3);for(let i=0;i<n;i++){const r=2.1+Math.random()*2.2,a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1);pos[i*3]=r*Math.sin(b)*Math.cos(a);pos[i*3+1]=r*Math.sin(b)*Math.sin(a);pos[i*3+2]=r*Math.cos(b)}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const pts=new THREE.Points(geo,new THREE.PointsMaterial({color:0x5578ff,size:.018,transparent:true,opacity:.72}));scene.add(pts);let mx=0,my=0;addEventListener('pointermove',e=>{mx=e.clientX/innerWidth-.5;my=e.clientY/innerHeight-.5});function resize(){const r=stage.getBoundingClientRect();renderer.setSize(r.width,r.height,false);camera.aspect=r.width/r.height;camera.updateProjectionMatrix()}resize();addEventListener('resize',resize);function loop(t){const s=Math.min(1,sy/900);group.rotation.y+=.0025+mx*.001;group.rotation.x+=.0012+my*.0007;group.position.y=Math.sin(t*.0007)*.08-s*.65;group.scale.setScalar(1+s*.17);pts.rotation.y-=.00035;camera.position.z=7-s*.65;renderer.render(scene,camera);requestAnimationFrame(loop)}requestAnimationFrame(loop)}$$('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{if(innerWidth<800)return;const r=el.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;el.style.transform=`perspective(900px) rotateX(${-y/r.height*7}deg) rotateY(${x/r.width*8}deg) translateZ(10px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});const modes={fluid:['FLUID EXPERIENCE','حرکت و فاصله‌ها با اندازه صفحه و رفتار کاربر سازگار می‌شوند؛ هویت بصری ثابت می‌ماند.'],minimal:['MINIMAL PRECISION','کمترین عناصر ممکن، بیشترین تمرکز؛ تایپوگرافی و فضای خالی نقش اصلی را دارند.'],progressive:['PROGRESSIVE WEB','حس یک محصول واقعی و سریع؛ رابط وب با رفتار نزدیک به یک اپلیکیشن.'],dynamic:['DYNAMIC INTERACTION','کلیک، hover، اسکرول و micro-interactionها بخشی از داستان تجربه هستند.'],responsive:['RESPONSIVE SYSTEM','چیدمان برای دسکتاپ، تبلت و موبایل از ابتدا طراحی می‌شود.'],fixed:['FIXED ART DIRECTION','یک قاب کنترل‌شده و دقیق برای ترکیب‌بندی‌هایی که باید کاملاً تحت کنترل باشند.']};$$('.mode').forEach(b=>b.addEventListener('click',()=>{const d=modes[b.dataset.mode];$$('.mode').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#modeTitle').textContent=d[0];$('#modeText').textContent=d[1];$('#modeNum').textContent=b.querySelector('b').textContent}));const modal=$('#modal');$$('.project').forEach(p=>p.addEventListener('click',e=>{if(e.target.closest('a'))return;$('#modalTitle').textContent=p.dataset.title;$('#modalText').textContent=p.dataset.copy;modal.classList.add('open');document.body.style.overflow='hidden'}));function close(){modal.classList.remove('open');document.body.style.overflow=''}$('#close')?.addEventListener('click',close);modal?.addEventListener('click',e=>e.target===modal&&close());addEventListener('keydown',e=>e.key==='Escape'&&close());$$('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>$(a.getAttribute('href'))?.scrollIntoView({behavior:'smooth'})) )})();