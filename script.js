(()=>{const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)],root=document.documentElement;const loader=$('#loader');setTimeout(()=>{if(loader){loader.style.opacity='0';loader.style.pointerEvents='none';setTimeout(()=>loader.remove(),500)}},700);let mx=.5,my=.5,scrollY=0;addEventListener('pointermove',e=>{mx=e.clientX/innerWidth-.5;my=e.clientY/innerHeight-.5;root.style.setProperty('--mx',e.clientX+'px');root.style.setProperty('--my',e.clientY+'px')},{passive:true});$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{if(innerWidth<900)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;el.style.transform=`translate3d(${x*5}px,${y*4}px,0)`});el.addEventListener('pointerleave',()=>el.style.transform='')});$$('.reveal').forEach(el=>new IntersectionObserver(es=>es.forEach(x=>x.isIntersecting&&x.target.classList.add('visible')),{threshold:.08}).observe(el));const progress=$('.progress');addEventListener('scroll',()=>{scrollY=scrollY*.75+window.scrollY*.25;const p=Math.min(1,window.scrollY/Math.max(1,document.body.scrollHeight-innerHeight));root.style.setProperty('--progress',p);if(progress)progress.style.transform=`scaleX(${p})`},{passive:true});
const canvas=$('#heroCanvas'),stage=$('#stage');if(canvas&&stage){const ctx=canvas.getContext('2d',{alpha:true});let w=0,h=0,dpr=1,particles=[];function resize(){const r=stage.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.5);w=r.width;h=r.height;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);particles=Array.from({length:Math.min(260,Math.max(120,Math.floor(w*h/2400)))},()=>{const z=Math.random()*2-1,a=Math.random()*Math.PI*2,rr=Math.sqrt(1-z*z);return{x:Math.cos(a)*rr,y:Math.sin(a)*rr,z,phase:Math.random()*6.28}})}resize();addEventListener('resize',resize);function draw(t){ctx.clearRect(0,0,w,h);const cx=w*.5+mx*24,cy=h*.49+my*18,base=Math.min(w,h)*.24,spin=t*.00022,sy=Math.min(1,window.scrollY/1000),items=[];for(const p of particles){const a=Math.atan2(p.y,p.x)+spin,rr=Math.hypot(p.x,p.y),x=p.x*Math.cos(spin)-p.y*Math.sin(spin),y=p.x*Math.sin(spin)+p.y*Math.cos(spin),depth=(p.z+1)/2,size=1+depth*2;items.push({x:cx+x*base*(1+sy*.12),y:cy+y*base*(1+sy*.12),z:depth,size})}items.sort((a,b)=>a.z-b.z);for(const q of items){ctx.globalAlpha=.12+q.z*.65;ctx.beginPath();ctx.arc(q.x,q.y,q.size,0,Math.PI*2);ctx.fillStyle=q.z>.68?'#3d6cff':'#38c9e8';ctx.fill()}ctx.globalAlpha=.12;ctx.beginPath();ctx.arc(cx,cy,base*1.16,0,Math.PI*2);ctx.strokeStyle='#5578ff';ctx.lineWidth=1;ctx.stroke();ctx.globalAlpha=.18;ctx.beginPath();ctx.arc(cx,cy,base*.72,0,Math.PI*2);ctx.strokeStyle='#20c8e8';ctx.stroke();ctx.globalAlpha=1;requestAnimationFrame(draw)}requestAnimationFrame(draw)}
$$('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{if(innerWidth<900)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1000px) rotateX(${-y*3.5}deg) rotateY(${x*4}deg) translateY(-4px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
const modes={fluid:['FLUID EXPERIENCE','حرکت و فاصله‌ها با اندازه صفحه و رفتار کاربر سازگار می‌شوند؛ هویت بصری ثابت می‌ماند.'],minimal:['MINIMAL PRECISION','کمترین عناصر ممکن، بیشترین تمرکز؛ تایپوگرافی و فضای خالی نقش اصلی را دارند.'],progressive:['PROGRESSIVE WEB','حس یک محصول واقعی و سریع؛ رابط وب با رفتار نزدیک به یک اپلیکیشن.'],dynamic:['DYNAMIC INTERACTION','کلیک، hover، اسکرول و micro-interactionها بخشی از داستان تجربه هستند.'],responsive:['RESPONSIVE SYSTEM','چیدمان برای دسکتاپ، تبلت و موبایل از ابتدا طراحی می‌شود.'],fixed:['FIXED ART DIRECTION','یک قاب کنترل‌شده و دقیق برای ترکیب‌بندی‌هایی که باید کاملاً تحت کنترل باشند.']};$$('.mode').forEach(b=>b.addEventListener('click',()=>{const d=modes[b.dataset.mode];$$('.mode').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#modeTitle').textContent=d[0];$('#modeText').textContent=d[1];$('#modeNum').textContent=b.querySelector('b').textContent}));
const modal=$('#modal');$$('.project').forEach(p=>p.addEventListener('click',e=>{if(e.target.closest('a'))return;$('#modalTitle').textContent=p.dataset.title;$('#modalText').textContent=p.dataset.copy;modal.classList.add('open');document.body.style.overflow='hidden'}));function close(){modal?.classList.remove('open');document.body.style.overflow=''}$('#close')?.addEventListener('click',close);modal?.addEventListener('click',e=>e.target===modal&&close());addEventListener('keydown',e=>e.key==='Escape'&&close());$$('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>$(a.getAttribute('href'))?.scrollIntoView({behavior:'smooth'})));})();